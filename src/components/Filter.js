import React from "react";

export default class Filter extends React.Component {
  render() {
    return (
      <React.Fragment>
        <button>all</button>
        <button>not duing</button>
        <button>alredi duing</button>
      </React.Fragment>
    );
  }
}
